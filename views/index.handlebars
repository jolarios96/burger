<h1>Burgers!</h1>

<h2>Burgers that are not Eaten!</h2>

<ul>
    {{#each burgers}}
    {{#unless eaten}}
    {{> burgers/hamburger-block ate=true}}
    {{/unless}}
    {{/each}}
</ul>

<h2>burgers that are Eaten!</h2>

<ul>
    {{#each burgers}}
    {{#if eaten}}
    {{> burgers/hamburger-block ate=false}}
    {{/if}}
    {{/each}}
</ul>

<h2>Add a Hamburger</h2>
<form class="create-form">

    <div class="form-group">
        <label for="ca">Hamburger Name:</label>
        <input type="text" id="ca" name="name">
    </div>

    <div class="form-group">
        <label for="eaten">eaten?</label><br>
        <input type="radio" name="eaten" value="1" checked> Eaten!<br>
        <input type="radio" name="eaten" value="0"> Looks Delicious!
    </div>

    <button type="submit">Add Hamburger</button>
</form>