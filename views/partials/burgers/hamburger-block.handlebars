<li>
    {{name}}

    <button class="change-state" data-id="{{id}}" data-newState="{{ate}}">
        {{#if ate}}EAT IT!{{else}}Reorder!{{/if}}
    </button>

    <button class="delete-burger" data-id="{{this.id}}">Digest!</button>
</li>