<li>
    {{name}}

    <button class="change-ate" data-id="{{id}}" data-newate="{{ate}}">
        {{#if sleep}}EAT IT!{{else}}REORDER!{{/if}}
    </button>

    <button class="delete-burger" data-id="{{this.id}}">Digest!</button>
</li>